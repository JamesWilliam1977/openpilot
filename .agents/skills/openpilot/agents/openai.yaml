interface:
  display_name: "Openpilot"
  short_description: "Repo-specific patterns and workflows for openpilot"
  default_prompt: "Use the openpilot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true